fn main() {
    println!("Hello, world!");
    // add base url and single api
    // add number of users
    // see virtual os implementation
    // check api helper - use a default one for now (rewuests or something)
}
